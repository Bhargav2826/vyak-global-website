{
  "name": "vyak-global",
  "version": "1.0.0",
  "description": "Vyak Global Monorepo",
  "scripts": {
    "start": "node server/server.js",
    "build": "npm install --prefix client && npm run build --prefix client && npm install --prefix server"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
